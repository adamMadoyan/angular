'use strict';

/**
 * @namespace UserManagement.app.controllers
 * controller module of the app
 */
UserManagement.app.controllers = angular.module("UserManagement.app.controllers", []);

/**
 * @namespace UserManagement.app.services
 * services module of the app
 */
UserManagement.app.services = angular.module("UserManagement.app.services", []);

/**
 * @namespace UserManagement.app.directives
 * directives module of the app
 */
UserManagement.app.directives = angular.module("UserManagement.app.directives", []);

/**
 * @namespace UserManagement.app.filters
 * filters module of the app
 */
UserManagement.app.filters = angular.module("UserManagement.app.filters", []);

